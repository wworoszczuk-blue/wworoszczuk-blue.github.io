---
import Header from './Header.astro';
---

<section id="hero" class="relative min-h-screen flex flex-col justify-center items-center text-center px-8 md:px-16 lg:px-32 py-20 overflow-hidden">
  <Header />

  <div aria-hidden="true" class="absolute inset-0 z-0 pointer-events-none overflow-hidden">
  <!-- BACKGROUND GLOW -->
  <div class="absolute inset-0 bg-[radial-gradient(circle_at_center,_theme(colors.elivion.blue/0.3),transparent_90%)]"></div>

  <!-- GRID -->
  <svg class="absolute w-full h-full opacity-60" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="xMidYMid slice">
    <defs>
      <linearGradient id="gridGradient" x1="0%" y1="0%" x2="100%" y2="100%">
        <stop offset="0%" style="stop-color:#3A7BD5;stop-opacity:0.6" />
        <stop offset="50%" style="stop-color:#3A7BD5;stop-opacity:0.4" />
        <stop offset="100%" style="stop-color:#3A7BD5;stop-opacity:0.2" />
      </linearGradient>
      <pattern id="grid" width="80" height="80" patternUnits="userSpaceOnUse">
        <path d="M80 0L0 0 0 80" fill="none" stroke="url(#gridGradient)" stroke-width="0.5" opacity="0.8" vector-effect="non-scaling-stroke"/>
      </pattern>
    </defs>
    <rect width="100%" height="100%" fill="url(#grid)"/>
  </svg>
  
  <!-- FADE OUT GRADIENT -->
  <div class="absolute inset-0 bg-gradient-to-b from-transparent via-transparent via-70% to-elivion-bg"></div>
</div>

  <!-- CONTENT -->
  <div class="relative z-10 flex flex-col justify-center items-center space-y-10">

    <!-- HEADLINE -->
    <div class="space-y-6 animate-fade-in-up opacity-0 animation-delay-500">
      <h1 class="text-4xl md:text-5xl lg:text-6xl font-header leading-tight max-w-4xl">
        <span class="bg-clip-text">Transforming Corporations with</span>
        <span class="text-elivion-green font-bold">Human-Centered AI</span>
      </h1>
    </div>

    <!-- SUBHEADLINE -->
    <div class="animate-fade-in-up opacity-0 animation-delay-700">
      <div class="relative max-w-6xl text-slate-300 leading-relaxed text-xl md:text-2xl lg:text-3xl space-y-6">
        <p class="relative z-10 text-center">
          Elivion specializes in enterprise AI solutions that deliver
          <span class="inline-flex items-center gap-1 text-cyan-500 font-semibold">
            
            [ growth ]
          </span>,
          <span class="inline-flex items-center gap-1 text-teal-500 font-semibold">
            
            [ efficiency ]
          </span>, and
          <span class="inline-flex items-center gap-1 text-sky-500 font-semibold">
            
            [ innovation ]
          </span>.
          From strategy to execution, we support corporations in redefining how they learn, recruit, develop people, and future-proof their business with AI.
        </p>
      </div>
    </div>

    <!-- CTA -->
    <div class="sm:flex-row animate-fade-in-up opacity-0 animation-delay-900">
      <a href="#mission"
        class="group relative inline-flex items-center justify-center px-8 py-4 rounded-2xl font-semibold
               backdrop-blur-md bg-gradient-to-r from-emerald-400/20 via-cyan-400/20 to-emerald-400/20
               border border-white/10 shadow-[0_0_30px_rgba(0,255,170,0.15)]
               overflow-hidden transition-all duration-500 hover:scale-[1.03] hover:shadow-[0_0_45px_rgba(0,255,170,0.3)]">

        <!-- ANIMATED FLASH -->
        <span class="absolute inset-0 bg-gradient-to-r from-transparent via-emerald-400/40 to-transparent 
                     translate-x-[-100%] group-hover:translate-x-[100%] transition-transform duration-[1.2s] ease-in-out"></span>

        <span class="relative z-10 flex items-center gap-2">
          <span>Learn More</span>
          <span class="material-symbols-outlined">
            keyboard_double_arrow_down
          </span>
        </span>
      </a>
    </div>
  </div>
</section>


<style>

@keyframes fade-in-up {
  to { opacity: 1; transform: translateY(0); }
}

.animate-fade-in-up {
  animation: fade-in-up 0.8s ease-out forwards;
  transform: translateY(30px);
}

.animation-delay-300 { animation-delay: 0.3s; }
.animation-delay-500 { animation-delay: 0.5s; }
.animation-delay-700 { animation-delay: 0.7s; }
.animation-delay-900 { animation-delay: 0.9s; }
.animation-delay-1100 { animation-delay: 1.1s; }
</style>