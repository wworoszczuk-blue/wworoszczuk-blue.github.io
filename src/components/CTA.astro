---
// CTA.astro
---

<section id="contact" class="relative min-h-screen flex items-center justify-center px-8 md:px-16 lg:px-32 py-20 overflow-hidden text-white">

  <div class="relative z-10 w-full max-w-6xl mx-auto grid lg:grid-cols-2 gap-16 items-center">

      <!-- LEFT COLUMN -->
      <div class="space-y-8">
        <div>
          <span class="inline-block text-elivion-green text-sm font-mono uppercase tracking-widest mb-4">
            Get Started
          </span>
          <h2 class="text-4xl md:text-5xl lg:text-6xl font-header leading-tight">
            Ready to Transform Your Organization with <span class="text-elivion-green">AI</span>?
          </h2>
        </div>

      </div>

      <!-- RIGHT COLUMN: -->
      <div class="relative">
        <!-- Form Container -->
        <div class="relative p-8 bg-gradient-to-br from-white/5 to-white/[0.02] border border-white/10 rounded-3xl backdrop-blur-xl shadow-2xl">

          <form class="space-y-6" action="/submit" method="POST">
            <div class="space-y-2">
              <label for="name" class="block text-sm font-medium text-slate-300">Full Name</label>
              <div class="relative">
                <input 
                  type="text" 
                  id="name" 
                  name="name" 
                  required
                  class="w-full px-4 py-3 bg-white/5 border border-white/10 rounded-xl text-white placeholder-slate-400 focus:outline-none focus:border-elivion-green focus:ring-1 focus:ring-elivion-green transition-all duration-300"
                  placeholder="Enter your full name"
                />
                <div class="absolute inset-0 border border-elivion-green rounded-xl opacity-0 focus-within:opacity-100 transition-opacity duration-300 pointer-events-none"></div>
              </div>
            </div>

            <div class="space-y-2">
              <label for="email" class="block text-sm font-medium text-slate-300">Email</label>
              <div class="relative">
                <input 
                  type="email" 
                  id="email" 
                  name="email" 
                  required
                  class="w-full px-4 py-3 bg-white/5 border border-white/10 rounded-xl text-white placeholder-slate-400 focus:outline-none focus:border-elivion-green focus:ring-1 focus:ring-elivion-green transition-all duration-300"
                  placeholder="your@company.com"
                />
                <div class="absolute inset-0 border border-elivion-green rounded-xl opacity-0 focus-within:opacity-100 transition-opacity duration-300 pointer-events-none"></div>
              </div>
            </div>

            <div class="space-y-2">
              <label for="message" class="block text-sm font-medium text-slate-300">How can we help?</label>
              <div class="relative">
                <textarea 
                  id="message" 
                  name="message" 
                  required
                  class="h-24 sm:h-32 md:h-40 w-full px-4 py-3 bg-white/5 border border-white/10 rounded-xl text-white placeholder-slate-400 focus:outline-none focus:border-elivion-green focus:ring-1 focus:ring-elivion-green resize-none transition-all duration-300"
                  placeholder="Tell us about your AI challenges and goals..."
                ></textarea>
                <div class="absolute inset-0 border border-elivion-green rounded-xl opacity-0 focus-within:opacity-100 transition-opacity duration-300 pointer-events-none"></div>
              </div>
            </div>

            <button
              type="submit"
              class="group relative w-full py-4 px-6 bg-gradient-to-r from-elivion-green to-cyan-500 text-elivion-bg font-semibold rounded-xl transition-all duration-300 hover:scale-105 hover:shadow-2xl hover:shadow-elivion-green/25 focus:outline-none focus:ring-2 focus:ring-elivion-green focus:ring-offset-2 focus:ring-offset-[#020617]"
            >
              <span class="relative z-10">Get in Touch</span>
              <div class="absolute inset-0 bg-gradient-to-r from-cyan-500 to-elivion-green rounded-xl opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
              
              <svg class="absolute right-6 top-1/2 transform -translate-y-1/2 w-5 h-5 group-hover:translate-x-1 transition-transform duration-300" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 7l5 5m0 0l-5 5m5-5H6" />
              </svg>
            </button>
          </form>
        </div>
      </div>
  </div>
</section>

<style>

.animate-float {
  animation: float 8s ease-in-out infinite;
}

.animate-float-delayed {
  animation: float-delayed 10s ease-in-out infinite;
}

input:focus, textarea:focus {
  background: rgba(255, 255, 255, 0.08) !important;
}

button, input, textarea, a {
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.backdrop-blur-xl {
  backdrop-filter: blur(20px);
}
</style>